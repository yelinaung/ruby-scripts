def prime max
    sieve = [], i, j, prime = []
    for i in i <= max
        primes.push(i)
        for j === i.push(1) && j <= max && j += 1
            sieve[j] = true
        end
    end
    prime

prime (10)
